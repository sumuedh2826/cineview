import { PlaceholderPage } from '@/Common'

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      subtitle="Auth module — sign-in form arrives in Milestone 2."
    />
  )
}