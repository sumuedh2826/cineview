import { PlaceholderPage } from '@/Common'

export function SearchPage() {
  return (
    <PlaceholderPage
      title="Search"
      subtitle="Search module — query UI arrives in later milestones."
    />
  )
}